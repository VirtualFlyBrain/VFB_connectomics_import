.idea
**/.DS_Store
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.env
synapses_v1_human_readable_sizethresh3_connectioncounts_countthresh3.parquet
.venv/
dashboard/site/
dashboard/__pycache__/

# Large generated/source artifacts — OWL outputs are committed compressed
# (*.owl.gz / *.owl.zip) in src/VFB_connectomics_import/OWL/, never as raw multi-GB files.
*.owl
*_n_2_n.tsv
* copy.*
dashboard/STATUS.md

# Baked transform fields (~543 MB) — built by bake_banc_fields.py, published alongside
# VFB's other transforms, never committed. See TRANSFORMS.md.
banc_transform_fields/
*_2um.npy
*_2um.npz

# Jenkins workspace outputs — the workspace is a checkout, so these land in the tree
ledger-*.jsonl
report-*.csv
archive/
compare/
