vfb_connect
neuprint-python
pandas
python-catmaid
jsonpath-rw
pyyaml
fafbseg
certifi

# transform / image pipeline (see TRANSFORMS.md)
flybrains>=0.6.3      # BANC support arrived in 0.6.x; 0.3.0 has none
navis                 # transform registry + xform_brain
trimesh               # mesh IO and plane slicing
# Quadric decimation of the served OBJs (docs/ISSUES.md IMG-1). MUST be version-marked:
# 0.2.0's metadata says requires-python >=3.9 but its code uses PEP 604 unions
# (`float | None` in simplify.py), which is a SyntaxError-at-import on 3.9. Every 0.1.x is
# clean. pyproject.toml reads this file as its base dependencies, so an unpinned entry here
# overrides the marker in the [images] extra — pin it in both places or neither works.
# Pinned EXACTLY. Root cause established 2026-08-27: fast-simplification 0.1.13 and
# 0.2.0 both declare `requires-python >=3.9` but ship no cp39 wheel and their source uses
# PEP 604 unions (`float | None`), so on the Jenkins node (3.9.2) pip builds them from
# sdist and `import fast_simplification` then raises TypeError. `<0.2` was not enough
# because it still selects 0.1.13. 0.1.12 is the newest with a cp39 wheel and 3.9-safe
# source; it works on 3.10+ too, and our single simplify(v, f, target_reduction=...) call
# is unchanged across 0.1.x. Do not relax this without checking the wheel tags AND the
# source of whatever version you move to.
fast-simplification==0.1.12
shapely               # required by trimesh.slice_plane, even with cap=False
scipy                 # map_coordinates for the baked-field lookup
cloud-volume          # BANC precomputed mesh fetch
skeletor              # skeletonise the mesh for the 40.7% of roots with no published _skeleton
pynrrd                # NRRD output for the VFB stack viewer
numpy                 # pulled in by navis anyway, but imported directly throughout
plotly                # old-vs-new 3D comparison pages (banc_compare_html.py)
